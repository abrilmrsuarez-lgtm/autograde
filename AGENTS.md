# Autograde Project - Agent Instructions

## Dependency Management
Use `uv` for all dependency management.

## Testing
Use `pytest` for all testing. Run tests with: `uv run pytest tests/ -v`

## Test Organization
All tests must be contained in a `tests/` folder at the root of the project.
