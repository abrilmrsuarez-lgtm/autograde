Create a public GitHub repository named autograde. The project should be a Python CLI tool that accepts a valid repository URL as input and evaluates whether the following conditions are met:

1. The target is a valid Git repository,
2. The main branch exists,
3. The feature branch exists on the remote,
4. The file1.txt is present on the main branch.